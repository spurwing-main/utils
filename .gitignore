############################################
# Node/JS ecosystem
############################################
node_modules/
.npm-cache/
.npm-cache-tmp/
.pnpm-store/

# Lockfiles
# We track npm's package-lock.json for deterministic CI installs and caching.
# If using other managers, keep their lockfiles untracked unless you intend to switch.
# package-lock.json (tracked)
# pnpm-lock.yaml (ignored by default)
# yarn.lock (ignored by default)
pnpm-lock.yaml
yarn.lock

# Logs & caches
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.eslintcache
.cache/
.nyc_output/

# Build outputs
dist/
build/
coverage/
.parcel-cache/
.turbo/
out/

# Framework-specific
.next/
.vercel/

# Env & local config
.env
.env.*
.env.local
.env.*.local
.npmrc

# OS/editor noise
.DS_Store
.idea/
.vscode/
Thumbs.db
